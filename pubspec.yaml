name: alkhair_cafey
description: تطبيق كافتيريا الخير - نسخته المبدئية
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.10.0
  cloud_firestore: ^4.6.0
  firebase_auth: ^4.4.0
  firebase_storage: ^11.1.0
  provider: ^6.0.5
  url_launcher: ^6.1.10
  geolocator: ^10.4.0
  intl: ^0.18.0
  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
